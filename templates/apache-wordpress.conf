RewriteEngine On
RewriteRule ^{{ wordpress_url }}/index\.php$ - [passthrough]
RewriteRule ^{{ wordpress_url }}/wp- - [passthrough]
RewriteRule ^{{ wordpress_url }}/xmlrpc\.php$ [passthrough]
RewriteRule ^{{ wordpress_url }}($|/) {{ wordpress_url }}/index.php [passthrough]
{% if wordpress_url == '' %}
Alias / /usr/local/wordpress-{{ wordpress_installation_name }}/
{% else %}
Alias {{ wordpress_url }} /usr/local/wordpress-{{ wordpress_installation_name }}
{% endif %}
<Directory /usr/local/wordpress-{{ wordpress_installation_name }}>
  Require all granted
  Options FollowSymLinks
  AllowOverride Limit Options FileInfo
  DirectoryIndex index.php
</Directory>
